version: '3.8'

services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: yt_downloader_backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped
